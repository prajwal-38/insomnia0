# Insomnia Video Editor - Environment Configuration
# Copy this file to .env.local and fill in your values

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# Backend API Configuration
VITE_API_URL=http://localhost:8080

# AI Processing Configuration
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# Environment Settings
VITE_ENVIRONMENT=development
VITE_NODE_ENV=development

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================
#
# IMPORTANT: For backend configuration, copy backend/.env.example to backend/.env
# The backend has its own environment file with additional settings for:
# - Database configuration (SQLite/PostgreSQL)
# - AI processing APIs (Gemini, AssemblyAI)
# - Cloud storage settings
# - Audio translation services
# - Remotion renderer settings
#
# This consolidated approach ensures all backend services (main API, audio translator,
# remotion renderer) can access the same environment variables.